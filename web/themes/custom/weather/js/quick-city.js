// (function ($, Drupal) {
//   Drupal.behaviors.weatherQuickCity = {
//     attach: function (context, settings) {
//       $('.quick-btn', context).once('weatherQuickCity').on('click', function () {
//         var cityName = $(this).data('city');

//         $('.search-input', context).val(cityName);

//         $('.weather-search-form', context).trigger('submit');
//       });
//     }
//   };
// })(jQuery, Drupal);
